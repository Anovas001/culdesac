import type { Tournament } from "@/generated/prisma/client";
import { saveTournament } from "@/app/admin/actions";

export function TournamentForm({ tournament }: { tournament?: Tournament }) { const t = tournament; return <form action={saveTournament} className="card form"><input type="hidden" name="id" value={t?.id ?? ""}/><label>Nom<input name="name" defaultValue={t?.name} required /></label><label>Slug<input name="slug" defaultValue={t?.slug} required /></label><label>Titular<input name="headline" defaultValue={t?.headline} required /></label><label>Data i hora<input name="eventDate" type="datetime-local" defaultValue={t ? new Date(t.eventDate).toISOString().slice(0,16) : ""} required /></label><div className="two"><label>Preu (cèntims)<input name="priceCents" type="number" min="1" defaultValue={t?.priceCents ?? 1500} required /></label><label>Moneda<input name="currency" defaultValue={t?.currency ?? "eur"} maxLength={3} required /></label></div><label>Capacitat (opcional)<input name="capacity" type="number" min="1" defaultValue={t?.capacity ?? ""} /></label><label>URL imatge hero (opcional)<input name="heroImageUrl" type="url" defaultValue={t?.heroImageUrl ?? ""} /></label><label>Descripció<textarea name="description" defaultValue={t?.description} required /></label><label>Normes<textarea name="rules" defaultValue={t?.rules} required /></label><label>Estat<select name="status" defaultValue={t?.status ?? "DRAFT"}>{["DRAFT","OPEN","CLOSED","COMPLETED"].map(status => <option key={status}>{status}</option>)}</select></label><button>Desar torneig</button></form>; }
